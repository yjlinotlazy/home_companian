# 家宠设计文档

## 产品目标

家宠是运行在家庭电子墨水屏上的只读公告栏。服务端为 ESP32 提供可直接显示的画面，同时提供网页预览。

第一版优先显示打卡项目。暂不支持确认、取消、领取任务或记录完成状态，只要求设备能够定期获取并刷新内容。

## 硬件约束

- 屏幕：5.79 inch 黑白 e-ink，横屏显示。
- 可见分辨率：792x272。
- 控制器显存尺寸：800x272，每像素 1 bit，共 27,200 字节。
- 屏幕由两个级联 SSD1683 控制器驱动，中间包含 8 像素不可见接缝。
- 当前驱动要求 framebuffer 包含接缝映射及 180 度旋转。
- 刷新后电子墨水屏会保持画面，不需要服务端持续推送。

参考硬件测试项目：`/home/yli/Embedded/esp32/crowpanel-579-epaper`。

## 第一版架构

### 服务端

使用 Python，HTTP 层使用标准库 `ThreadingHTTPServer`，减少第一版的外部依赖：

- Pillow 负责中文排版、黑白二值化及 framebuffer 转换。
- YAML 存储用户配置和打卡项目。
- HTTP 服务同时提供设备画面和网页预览。

计划提供以下接口：

- `GET /display.bin?battery=82`：返回 `application/octet-stream`，响应体必须恰好为 27,200 字节。
- `GET /preview.png`：返回与设备画面内容一致的 PNG 预览图。
- `GET /`：提供简单网页预览和手动刷新入口。

设备接口直接返回排版完成的 framebuffer。ESP32 不负责解析 JSON、字体排版或 PNG 解码。

### ESP32

设备按配置的间隔主动请求 `display.bin`。一次刷新流程为：

1. 连接网络。
2. 在请求中提交当前电量。
3. 下载 framebuffer。
4. 验证 HTTP 状态和响应长度。
5. 将内容交给现有 `EPD_Display()` 刷新。
6. 屏幕和设备进入休眠，等待下一次刷新。

下载失败或响应长度错误时不清屏，保留上一次画面。

## 内容模型

第一版的打卡项目只包含稳定标识、标题和可选说明：

```yaml
items:
  - id: walk
    title: 散步
    description: 出门走一走
```

同一个项目可以被定时模式引用，也可以加入随机项目池。

## 内容选择模式

`scheduled` 和 `random` 是两种全局模式，通过配置切换，第一版不混用。

### 定时模式

定时配置通过项目 `id` 引用内容：

```yaml
mode: scheduled
schedule:
  - time: "12:00"
    item: walk
  - time: "13:00"
    item: read
```

每次设备请求时，服务端根据当前本地时间选择最近一个已经开始的项目。例如 12:40 请求时仍选择 12:00 的项目。画面保持到设备下一次成功刷新，不需要单独的持续时间或过期时间字段。

### 随机模式

```yaml
mode: random
refresh_minutes: 60
random_items:
  - walk
  - read
```

设备每到刷新间隔请求一次，服务端从项目池选择一个项目。尽量避免连续两次显示相同项目。

`refresh_minutes` 描述设备请求周期；服务端不按该间隔主动推送。

## 页面内容

第一版画面包含：

- 打卡项目标题。
- 可选说明。
- 当前时间。
- 设备电量。

第一版暂不加入项目图片、动画和交互控件。

## 配置位置

用户配置默认存放在：

```text
~/.config/home_companian/config.yaml
```

## 非目标

以下内容不属于第一版：

- 打卡完成记录。
- 确认、取消、方向键或菜单交互。
- 家务领取及奖币。
- 相册、宠物、植物、数学题等其他内容类型。
- 服务端定时推送。
- 多种屏幕规格适配。

## 待确认

- ESP32 的 Wi-Fi 配置和服务器地址配置方式。
- 定时模式在当天第一条计划开始之前应显示哪个项目。
- 随机模式的“避免连续重复”状态保存在内存还是持久化。
- 状态栏的最终排版及缺少电量参数时的显示方式。
